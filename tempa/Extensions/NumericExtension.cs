using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CoffeeJelly.tempa.Extensions
{
    //public static class NumericExtension
    //{
    //    public static bool InRange(this float?[] value, float minValue, float maxValue)
    //    {
    //        value.All(e =>
    //        {
    //            if ((e.Value >= minValue) && (e.Value <= maxValue) || e.HasValue == false)
    //                return true;
    //            return false;

    //        });
    //    }
    //}
}
